# Copy to .env and fill in real values. Never commit .env (see .gitignore).

# Required for corrective_rag.ingestion (embedding calls).
OPENAI_API_KEY=

# Optional — defaults to the local Docker Compose Qdrant service.
QDRANT_URL=http://localhost:6333
