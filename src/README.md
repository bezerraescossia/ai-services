# src/

One directory per initiative/epic, e.g.:

- `src/corrective-rag/`
- `src/recommendation-system/`

Each initiative owns its own internal layout (package structure, notebooks,
scripts) — this repo doesn't impose a single top-level package.
